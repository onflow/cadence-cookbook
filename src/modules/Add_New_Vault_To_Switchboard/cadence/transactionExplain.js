export const transactionExplain = `Before executing this transaction you should first retrieve the capability to the token's vault you are willing to add to the switchboard, as is done in the template transaction. It's important to note that you would also have to import the FungibleToken interface, the FungibleTokenSwitchboard and ExampleToken (the token of which the vault you are adding to the switchboard) smart contracts.`;

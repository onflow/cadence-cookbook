export const transactionExplain = `To create a set, you first need to get a reference to the admin resource from the AuthAccount.

Once you receive that reference you can then create a set that gets stored in the sets and setsData dictionary.

`